ruby_tasks () {
  gem install bundler pry pry-doc method_source rubocop ruby_parser ruby-debug-ide solargraph
  gem update --system
  rbenv rehash
}

golang_tasks () {
 go get -u golang.org/x/tools/cmd/... github.com/rogpeppe/godef github.com/nsf/gocode github.com/golang/lint/golint
}

git_tasks () {
  git -C ~/.oh-my-zsh pull
}

node_tasks () {
  npm install -g eslint live-server elm-test elm-analyse elm-oracle elm-live uglify-js # create-elm-app tern ionic cordova cordova-res
}

ruby_tasks
# golang_tasks
git_tasks
node_tasks
gcloud components update
rustup update
asdf update
